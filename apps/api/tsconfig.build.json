{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  // Keep the production bundle lean: never emit tests or their helpers.
  "exclude": ["src/**/*.test.ts", "src/test/**"]
}
